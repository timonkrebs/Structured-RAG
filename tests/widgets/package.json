{
  "name": "structured-rag-widget-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Browser tests for the MCP widget HTML in StructuredRAG.Mcp/Widgets",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "build-fixture": "node tools/build-fixture.js"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
